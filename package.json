{
  "name": "studyflow-firestore-rules-tests",
  "private": true,
  "scripts": {
    "seed:public": "node scripts/seed_public_content.mjs",
    "test:rules": "node --test test/firestore/firestore_rules_test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
